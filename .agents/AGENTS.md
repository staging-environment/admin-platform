# Workspace Rules

## Deployment to Production
- Whenever completing a task or set of changes requested by the user, ALWAYS build assets, commit, push, and deploy to production automatically using `ddev deploy` (or `ddev deploy "<commit message>"`).
